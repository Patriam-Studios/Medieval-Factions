name: Release

on:
  release:
    types: [ created ]

permissions:
  contents: write

jobs:
  build-and-attach:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17 for Ponder
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Build Ponder dependency
        run: |
          git clone https://github.com/Dans-Plugins/Ponder.git /tmp/Ponder
          cd /tmp/Ponder
          git checkout 2.0.0
          chmod +x gradlew
          ./gradlew publishToMavenLocal

      - name: Set up JDK 25 for Medieval Factions
        uses: actions/setup-java@v4
        with:
          java-version: '25'
          distribution: 'temurin'

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Build Shadow JAR with Gradle
        run: ./gradlew clean shadowJar

      - name: Upload JAR to release
        uses: softprops/action-gh-release@v2
        with:
          files: build/libs/Medieval-Factions-*.jar
